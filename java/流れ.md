## 🗓 1週目｜環境構築 & 基本文法
**目標**：Javaで簡単なプログラムを書ける  

### 学習内容
- JDK / IDE（IntelliJなど）インストール
- Hello World
- 変数・データ型・演算子
- Scannerでの入力

 ToDo
```java
// 足し算プログラム
int a = 10;
int b = 5;
System.out.println(a + b); 
```
___

✏️ 2週目｜制御構文
目標：条件分岐と繰り返しが扱える

学習内容

if / else / switch

for / while / do-while

break / continue

ToDo// じゃんけん
```
// 九九表
for (int i = 1; i <= 9; i++) {
    for (int j = 1; j <= 9; j++) {
        System.out.print(i*j + " ");
    }
    System.out.println();
}  
```
___


🧱 3週目｜配列とメソッド

目標：配列 + 関数でコードを整理できる

学習内容

配列 / 拡張for

メソッド（戻り値 / 引数）

メソッド分割リファクタ

ToDo
```
// ランダム5個→最大値
import java.util.*;

Random r = new Random();
int[] nums = new int[5];
for (int i = 0; i < nums.length; i++) nums[i] = r.nextInt(100);

System.out.println(Arrays.stream(nums).max().getAsInt());  
```
___

🧠 4週目｜オブジェクト指向①

目標：クラス / インスタンスを理解

学習内容

フィールド・メソッド

コンストラクタ

static

ToDo
```
class Dog {
    String name;
    Dog(String name) { this.name = name; }
    void bark() { System.out.println(name+"がワン！"); }
}  
```
___
🏗 5週目｜オブジェクト指向②（応用）

目標：継承 / インターフェースを使える

学習内容

継承

オーバーライド

抽象クラス

interface

ToDo
```
abstract class Animal { abstract void cry(); }
class Cat extends Animal { void cry() { System.out.println("にゃー"); } }
class Dog extends Animal { void cry() { System.out.println("わん"); } }
```


チェックリスト

 継承とoverride理解

 設計を意識できる  

___
 ⚠️ 6週目｜例外処理 & ライブラリ

目標：エラーに強いコードが書ける

学習内容

try / catch / finally / throws

List / Map / Set

String / Math / LocalDateTime

ToDo
```
try {
    int a = 10 / 0;
} catch (Exception e) {
    System.out.println("エラー発生");
}
```


チェックリスト

 例外を理解

 List Map Set使えた  
___


🔌 7週目｜DB接続(JDBC)

目標：JavaとDBを接続する

学習内容

SQL基礎

INSERT / SELECT / UPDATE / DELETE

JDBC接続

ToDo
TodoアプリのDB連携


チェックリスト

 DBに接続

 データ保存できた  


---
 🌐 8週目｜Web（Servlet / JSP）

目標：JavaでWebアプリを動かせる

学習内容

MVC

Servlet → JSP

Session / Request / Response

Final課題
ログイン付きTodoアプリを作成

ログイン  
CRUD機能

---
🏎 9週目｜ラムダ式 / Stream API

目標：コレクション処理を簡潔に書ける

学習内容

ラムダ式 () -> {}

Stream API

filter / map / reduce / sorted

コレクション処理の高速化

ToDo
```
List<Integer> nums = List.of(1,2,3,4,5);
int sum = nums.stream()
              .filter(n -> n%2==1)
              .mapToInt(n -> n)
              .sum();
System.out.println(sum); // 奇数合計
```

ラムダ式書けた  
Streamで集計

---
🚀 10週目｜Web API & JSON

目標：JavaでAPIを叩ける・扱える

学習内容

HTTP / REST / JSON

GET / POST / API設計

JavaでAPIを呼ぶ（HttpClient / OkHttp）

読み取ったJSONをパース

ToDo
```
HttpClient client = HttpClient.newHttpClient();
HttpRequest req = HttpRequest.newBuilder()
    .uri(URI.create("https://api.github.com"))
    .build();
HttpResponse<String> res = client.send(req, HttpResponse.BodyHandlers.ofString());
System.out.println(res.body());
```

API通信成功  
JSON解析